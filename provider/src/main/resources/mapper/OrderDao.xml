<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.provider.dao.OrderDao">
    <insert id="insertOrder" parameterType="com.api.Bean.Order">
        insert into orders(goodid,des) values(#{goodId},#{desc})
    </insert>
</mapper>